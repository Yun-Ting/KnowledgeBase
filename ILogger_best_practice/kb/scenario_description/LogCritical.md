# ILogger `LogCritical()` extension methods

```csharp
LogCritical(ILogger, EventId, Exception, String, Object[])   // Formats and writes a critical log message.
LogCritical(ILogger, EventId, String, Object[])              // Formats and writes a critical log message.
LogCritical(ILogger, Exception, String, Object[])            // Formats and writes a critical log message.
LogCritical(ILogger, String, Object[])                       // Formats and writes a critical log message.
```
